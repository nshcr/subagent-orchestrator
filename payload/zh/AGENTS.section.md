## 子代理与并行

- 默认使用单个代理。仅在用户要求委派、有界叶子任务能够替代主代理的实质工作或隔离嘈杂证据，或需要独立高风险门禁时使用 `$subagent-orchestrator`。小型、顺序性、含糊、共享状态或协调成本高的工作由主代理完成。
- 主代理保留授权、范围、整合、审查发现裁决和最终验收权。子代理必须是叶子：先启动一个；只有任务有界、相互独立且所有权安全时才增加普通首轮的第二个；最多保留一个活动写入者，写入范围不得重叠，并使用已安装的模型与推理设置。角色选择、交接、后续波次、审查和基于证据的收尾由 Skill 规定。

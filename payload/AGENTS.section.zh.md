## 子代理与并行

- 默认使用单个代理。仅当用户明确要求委派，或某个有界叶子任务能够替代主代理的实质工作、隔离噪声较大的证据流，或提供必需的独立高风险门禁时，才使用 `$subagent-orchestrator`。小型工作优先直接调用或批量调用工具；如果主代理主要在协调、轮询或等待，就由主代理直接完成。
- 主代理保留授权、范围、整合、审查发现裁决和最终验收权。子代理始终是叶子节点；允许委派时只启动一个子代理，最多保留一个活动写入者且写入范围不得重叠，并使用已安装的模型与推理设置。遵循 Skill 的角色准入、不可变叶子边界、扩展检查点、审查收敛和基于证据完成闭环的规则。

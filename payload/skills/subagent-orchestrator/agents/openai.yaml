interface:
  display_name: "Subagent Orchestrator"
  short_description: "Bounded delegation with primary-owned convergence"
  default_prompt: "Use $subagent-orchestrator to keep delegation bounded, clear expansion only after re-anchoring and new evidence, and keep reviewer findings advisory to the primary."

policy:
  allow_implicit_invocation: true

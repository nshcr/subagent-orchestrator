interface:
  display_name: "Subagent Orchestrator"
  short_description: "Bounded delegation with primary-owned decisions"
  default_prompt: "Use $subagent-orchestrator to delegate only bounded leaf work, keep expansion evidence-driven, and retain review decisions in the primary."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Subagent Orchestrator"
  short_description: "Efficient delegation with bounded expansion"
  default_prompt: "Use $subagent-orchestrator to start primary-only, prefer one bounded child for material or noisy work, stop unapproved expansion and reviewer-driven redesign, and keep task closure evidence-based."

policy:
  allow_implicit_invocation: true
